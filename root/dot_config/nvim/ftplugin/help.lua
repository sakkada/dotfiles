-- local cmd = vim.api.nvim_command

-- local skkdlib = require('sakkadalib')  -- not usable now

print('help file ft plugin')
-- cmd('setlocal fileformat=dos')     -- formats that will be tried when starting to edit buffer
-- cmd('setlocal fileformats=dos')     -- formats that will be tried when starting to edit buffer
